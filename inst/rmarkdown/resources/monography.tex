% ---------------------------------------------------------------------------
% nordTemplates :: Monography master thesis (Nord University Business School)
% Compile with xelatex (Times New Roman from the system; falls back to the
% free newtxtext/newtxmath Times-clone if Times New Roman is not installed).
% ---------------------------------------------------------------------------
\documentclass[$if(layout)$$layout$,$endif$ openany, oneside, 12pt]{book}

% --- modern engine detection --------------------------------------------------
\usepackage{iftex}

% --- general utilities --------------------------------------------------------
\usepackage[hyphens]{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{setspace}
\usepackage{lastpage}
\usepackage{lineno}
\usepackage{appendix}
\usepackage{lipsum}             % demo "Lorem ipsum" filler used by the skeleton
\usepackage{float}              % "H" placement specifier for figures/tables
\usepackage{placeins}           % \FloatBarrier
\usepackage{caption}            % needed by kableExtra and for caption tweaks
\usepackage{multirow}           % multirow tables (kableExtra)
\usepackage{pdflscape}          % landscape pages for wide tables
\usepackage{threeparttable}     % kableExtra footnotes / table notes
\usepackage{tikz}
\usetikzlibrary{positioning, fit}
\usepackage[margin=2.5cm, a4paper]{geometry}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Pandoc >= 3.x wraps every image in \pandocbounded{...} to keep it inside
% the text block. Older custom templates (like this one) need to define the
% macro themselves; the definition below is the canonical Pandoc one.
\makeatletter
\@ifundefined{pandocbounded}{%
  \newsavebox\pandoc@box
  \newcommand*\pandocbounded[1]{%
    \sbox\pandoc@box{#1}%
    \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
    \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
    \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi
    \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
    \else\usebox{\pandoc@box}%
    \fi%
  }%
}{}
\makeatother

$if(linenumbers)$\linenumbers$endif$

% --- fonts: Times New Roman (approved) ---------------------------------------
% When compiled with xelatex/lualatex we use the system Times New Roman.
% On a clean Linux/TinyTeX install without TNR, fontspec emits a warning
% but the document still typesets with Latin Modern; users who want a
% Times look on such systems can comment the \setmainfont line below and
% uncomment the newtx block in the pdflatex branch.
\ifxetex
  \usepackage{fontspec}
  \defaultfontfeatures{Ligatures=TeX, Scale=MatchLowercase}
  \IfFontExistsTF{Times New Roman}
    {\setmainfont{Times New Roman}}
    {\setmainfont{TeX Gyre Termes}}   % free Times-clone shipped with TeX Live
\else\ifluatex
  \usepackage{fontspec}
  \defaultfontfeatures{Ligatures=TeX, Scale=MatchLowercase}
  \IfFontExistsTF{Times New Roman}
    {\setmainfont{Times New Roman}}
    {\setmainfont{TeX Gyre Termes}}
\else                                  % pdflatex fallback
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{newtxtext}
  \usepackage{newtxmath}
\fi\fi

\IfFileExists{microtype.sty}{\usepackage{microtype}}{}

% --- bibliography ------------------------------------------------------------
% By default pandoc uses citeproc; natbib/biblatex are only loaded if the user
% sets citation_package = "natbib" (or "biblatex") in the YAML / output format.
$if(natbib)$
\usepackage[$for(natbiboptions)$$natbiboptions$$sep$,$endfor$]{natbib}
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$apalike$endif$}
$endif$
$if(biblatex)$
\usepackage[$if(biblio-style)$style=$biblio-style$,$endif$$for(biblatexoptions)$$biblatexoptions$$sep$,$endfor$]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$

\usepackage[unicode,bookmarks=true,breaklinks=true,
            colorlinks=$if(colorlinks)$true$else$false$endif$,
            linkcolor=$if(linkcolor)$$linkcolor$$else$magenta$endif$,
            urlcolor=$if(urlcolor)$$urlcolor$$else$blue$endif$,
            pdfborder={0 0 0}]{hyperref}
\urlstyle{same}

$if(highlighting-macros)$$highlighting-macros$$endif$

\setcounter{secnumdepth}{4}

% --- localisation (Norwegian / English) ---------------------------------------
$if(lang)$
  \usepackage[main=$lang$]{babel}
  \def\MainTitle{MASTEROPPGAVE}
  \def\course{Emnekode: }
  \def\Candit{Kandidat }
  \def\School{Handelsh{\o}gskolen i Nord}
  \def\clocktime{Dato: }
  \def\Student{Navn / kandidatnr.: }
  \def\totPages{Totalt antall sider: }
  \renewcommand{\contentsname}{Innholdsfortegnelse}
  \renewcommand{\listtablename}{Oversikt over tabeller}
  \renewcommand{\listfigurename}{Oversikt over figurer}
  \def\logoPic{\includegraphics[height=1.7cm]{nordlogono}}
  \def\Litname{Litteraturliste}
  \def\akn{Forord}
  \renewcommand{\figurename}{Figur}
  \renewcommand{\tablename}{Tabell}
  \renewcommand{\equationautorefname}{Ligning}
  \renewcommand{\appendixtocname}{Appendiks}
$else$
  \def\MainTitle{MASTER THESIS}
  \def\course{Course code: }
  \def\clocktime{Date: }
  \def\Student{Name / Candidate no.: }
  \def\Candit{Candidate}
  \def\School{Nord University Business School}
  \def\totPages{Total number of pages: }
  \def\contentsname{Table of contents}
  \def\listtablename{List of tables}
  \def\listfigurename{List of figures}
  \def\logoPic{\includegraphics[height=1.65cm]{nordlogoen}}
  \def\Litname{References}
  \def\figurename{Figure}
  \def\tablename{Table}
  \def\akn{Preface}
  \def\equationautorefname{Equation}
  \renewcommand{\appendixtocname}{Appendices}
$endif$

% --- pandoc citeproc citations (Pandoc >= 2.11; current form for Pandoc 3.x).
$if(csl-refs)$
\NewDocumentCommand\citeproctext{}{}
\NewDocumentCommand\citeproc{mm}{%
  \begingroup\def\citeproctext{#2}\cite{#1}\endgroup}
\makeatletter
 \let\@cite@ofmt\@firstofone
 \def\@biblabel#1{}
 \def\@cite#1#2{{#1\if@tempswa , #2\fi}}
\makeatother
\newlength{\cslhangindent}\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]   % #1 hanging-ind., #2 entry spacing
  {\begin{list}{}{%
    \setlength{\itemindent}{0pt}%
    \setlength{\leftmargin}{0pt}%
    \setlength{\parsep}{0pt}%
    \ifodd #1
      \setlength{\leftmargin}{\cslhangindent}%
      \setlength{\itemindent}{-1\cslhangindent}%
    \fi
    \setlength{\itemsep}{#2\baselineskip}}}%
  {\end{list}}
\usepackage{calc}
\newcommand{\CSLBlock}[1]{\hfill\break\parbox[t]{\linewidth}{\strut\ignorespaces#1\strut}}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut#1\strut}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{\strut#1\strut}}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$

% --- pandoc header-includes from YAML -----------------------------------------
$for(header-includes)$
$header-includes$
$endfor$

% --- page style ---------------------------------------------------------------
\usepackage{fancyhdr}
\fancypagestyle{main}{%
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\iffloatpage{}{\thepage}}
  \fancyhead[L]{}
  \fancyhead[R]{}
}

\renewcommand\thechapter{}
\renewcommand\thesection{\arabic{section}}
\renewcommand\thesubsection{\thesection.\arabic{subsection}}
\renewcommand\thesubsubsection{\thesubsection.\arabic{subsubsection}}
\renewcommand\theparagraph{}
\renewcommand\thesubparagraph{}

\usepackage{tocloft,titlesec}
\titleformat{\chapter}[hang]
  {\normalfont\Large\bfseries}{\thechapter}{1em}{}
\titlespacing*{\chapter}{0pt}{3.5ex plus 1ex minus .2ex}{2.3ex plus .2ex}
\titleformat{\paragraph}[display]
  {\normalfont\bfseries\fillast}{{\theparagraph}}{1ex minus .1ex}{\small}
\titlespacing{\subsubsection}{3pc}{*3}{*2}[3pc]

% --- Nord colour palette ------------------------------------------------------
\definecolor{nordblue}{RGB}{0, 176, 185}
\definecolor{nordgrey}{RGB}{148, 183, 187}

\def\Appendices{$if(appendices)$\newpage\appendix\chapter*{\appendixtocname\label{app}}
  \addappheadtotoc\renewcommand\thesection{\Alph{section}}$endif$}

\def\MyRefs{\newpage\backmatter\chapter{\Litname}}

$preamble$

% =============================================================================
\begin{document}
\begin{titlepage}

\vskip1cm
\begin{tikzpicture}[remember picture, overlay]
  \fill[fill=nordblue!45!white] (-1.5,2) rectangle (16.5,-22);
  \node[text=white, scale=2, anchor=west] at (0,0) {\Huge\MainTitle};
  \fill[fill=white] (.5,-5) rectangle (18,-14);
  \draw[color=nordgrey!45!black, dotted, very thick] (1,-6.5) -- (16,-6.5);
  \node[anchor=west] at (1.5,-6)  {\large\course\textit{$course$}};
  \node[anchor=west] at (7.5,-6)  {\large\Student\textit{$author$}};
  \node[anchor=west, scale=2, text width=7cm, color=nordgrey!60!black]
       at (1.5,-9.8) {\Large $title$};
  \draw[color=nordgrey!45!black, very thick] (1,-12.5) -- (16,-12.5);
  \node[anchor=west] at (1.5,-13.2) {\large\clocktime\textit{$date$}};
  \node[anchor=west] at (7.5,-13.2) {\large\totPages\textit{\pageref{LastPage}}};
  \node[anchor=south west] at (0,-25.5) {\logoPic};
  \node[anchor=east, color=nordgrey!80!white] at (15.5,-25)
       {\Large\href{https://www.nord.no}{www.nord.no}};
\end{tikzpicture}
\thispagestyle{empty}
\end{titlepage}

\frontmatter
\newpage
\renewcommand{\thepage}{\roman{page}}
\setcounter{page}{1}

$if(sammendrag)$\chapter{Sammendrag}$sammendrag$$endif$
$if(preface)$\chapter{\akn}$preface$$endif$
$if(abstract)$\chapter{Abstract}$abstract$$endif$

$if(lists)$
  \newpage\listoffigures
  \addcontentsline{toc}{chapter}{\listfigurename}
  \listoftables
  \addcontentsline{toc}{chapter}{\listtablename}
$endif$

\newpage
\tableofcontents

\mainmatter
\renewcommand{\thepage}{\arabic{page}}
\setcounter{page}{1}
\pagestyle{main}
\onehalfspacing

$body$

$for(include-after)$
$include-after$
$endfor$
\end{document}
